[build]
  publish = "."
  command = ""
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Séquence email post-achat — toutes les heures
[functions."email-sequence"]
  schedule = "0 * * * *"
